# 当前任务

> Last updated: 2026-06-03

## 已完成

- [x] 创建 `legal-ocr` Skill 目录结构
- [x] 实现 PaddleOCR 与 MinerU 双后端
- [x] 实现自动路由和统一配置
- [x] 实现统一 Markdown 输出与 archive 归档
- [x] 实现基础法律后处理
- [x] 编写技能文档、输出结构说明、变更记录和决策记录
- [x] 同步公开发布索引
- [x] 将旧 OCR Skill 的 `.env` 配置复制到 `legal-ocr/config/.env`
- [x] 迁入 `pdf-processor` 中适合 Markdown 主链路的 PaddleOCR API 能力：变量别名、异步任务、模型选择和额外 payload
- [x] 将自动路由改为配置优先，并增加额度/频率等失败分类与候选后端回退
- [x] 增加保守型法律术语优化，覆盖常见 OCR 断字、异体字、主体标签和文书结构词
- [x] 增加保守型 OCR 硬换行整理，合并同一中文段落内的物理换行
- [x] 按 `skill-lint` 规范扁平化脚本目录、补充依赖防护和输入/输出说明
- [x] 将 `legal-ocr` 定位调整为通用 OCR 默认可用，并让法律术语优化按法律上下文自动触发
- [x] 移除 archive 内 `input/` 目录与输入副本复制逻辑，输入元信息改为通过 `metadata.json.source` 字段记录；archive 累计体积从持续膨胀改为仅取决于后端响应与 Markdown 长度

## 待办事项

- [ ] 增加真实卷宗、病历、票据和网页 URL 的回归测试样本
- [ ] 增加非法律 OCR 回归测试样本，验证不会误触发法律术语优化
- [ ] 基于真实样本扩充法律术语词表，并为高风险替换建立回归测试
- [ ] 基于真实普通文本和法律文本继续校准法律上下文检测阈值
- [ ] 基于真实样本校准硬换行合并规则，避免误合并短句、列表和表格
- [ ] 评估印章、图表和页眉页脚的后处理规则
- [ ] 根据真实样本校准自动路由顺序和失败回退阈值
- [ ] 若 PaddleOCR 或 MinerU 提供稳定额度查询接口，接入转换前 quota 预检
- [ ] 评估是否将 `pdf-processor` 的拍照件自动矫正迁入 archive 元数据或图片资源链路
