# 技术决策记录

本文档记录 `article2book` 技能设计过程中的关键决策与工作日志。

## [DEC-001] - 2026-04-02 - 输出重点放在“成书判断 + 结构方案”，而不是文章摘要

### 背景

用户的真实需求不是简单汇总文章，而是判断一个既有文章目录能否被提炼为可出版的书籍项目，并给出结构化整合方案。

### 选项

1. **做成文章总结器**
 - 优点：实现简单
 - 缺点：无法回答“能否成书”和“怎么重组”的关键问题

2. **做成书稿策划器**
 - 优点：直接回应出版判断、目录结构、读者定位和删改建议
 - 缺点：需要更强的评估标准与输出模板

### 决策

选择 **选项 2：做成书稿策划器**。

### 理由

1. 用户已经有相当多现成内容，真正缺的是“收束”和“结构化”
2. 成书不是摘要工作，而是主题判断、读者定位和章节重组工作
3. 这样设计更符合文章资产再利用的真实场景

### 影响

- 标准输出改为书稿策划包，而不是摘要合集
- 增加成书可行性评估和目录映射两个核心文件

## [DEC-002] - 2026-04-02 - 采用“源目录优先，archive 回退”的输出策略

### 背景

用户希望技能对任意文章目录都可复用。如果把输出路径写死到某个固定目录，后续跨设备、跨目录使用会非常别扭。

### 选项

1. **固定输出到某个个人路径**
 - 优点：单机使用方便
 - 缺点：不可移植

2. **要求用户每次都手动指定输出路径**
 - 优点：灵活
 - 缺点：交互成本高

3. **源目录优先，archive 回退**
 - 优点：成书策划结果与原文章目录相邻，同时兼容不可写目录
 - 缺点：需要清楚说明双路径规则

### 决策

选择 **选项 3：源目录优先，archive 回退**。

### 理由

1. 书稿整理结果最好贴着原文章资产存放，方便反复修订
2. 回退策略能兼容只读目录和统一归档需求
3. 该方式比硬编码个人路径更利于复用

### 影响

- `SKILL.md` 中明确输出目录规则
- 新增 `archive/.gitkeep`

## [DEC-003] - 2026-04-02 - 增加标准库扫描脚本，先建立素材清单再做判断

### 背景

如果直接阅读大量素材全文再人工整理，成本高且容易漏项。先建立素材清单，有助于识别主题分布、规模和边缘内容。

### 选项

1. **只靠人工读取**
 - 优点：灵活
 - 缺点：面对几十篇文章时容易漏读、漏记

2. **增加轻量扫描脚本**
 - 优点：先把候选文章、标题、预览、规模统一索引出来
 - 缺点：需要维护一个小脚本

### 决策

选择 **选项 2：增加轻量扫描脚本**。

### 理由

1. 先盘点素材资产，再做主题判断，更符合内容策划流程
2. 标准库脚本没有额外依赖，适合本地快速运行
3. 结构化索引可作为后续章节映射和删改判断的基础

### 影响

- 新增 `scripts/build_article_inventory.py`
- 标准输出中加入 `processed/article_inventory.csv/jsonl`

## [DEC-004] - 2026-04-02 - 技能以“现有素材重组”为主，不以“从零写书”为主

### 背景

用户进一步明确，这个技能的目标并不是从零生成一本书，而是基于已经存在的文章、视频教程、课程讲稿等素材，把内容重组为书籍形式。

### 选项

1. **保留“文章整理成书”的窄定义**
 - 优点：边界更简单
 - 缺点：无法覆盖视频教程、讲稿、逐字稿等真实来源

2. **扩展为“现有内容素材结构化成书”**
 - 优点：更贴近作者已有内容资产的真实形态
 - 缺点：需要明确非文本素材需先转写

### 决策

选择 **选项 2：扩展为“现有内容素材结构化成书”**。

### 理由

1. 很多可出版内容的起点并不是文章，而是课程、视频、分享或逐字稿
2. 真正的价值是“把已有表达重新编排为书籍结构”，不是“凭空产书”
3. 这样定义后，技能更适合作为内容资产再开发工具

### 影响

- `SKILL.md` 明确加入课程讲稿、视频教程逐字稿、演讲提纲等典型输入
- 标准输出改称“候选素材总表”
- 扫描脚本增加 `.srt`、`.vtt` 支持，并新增 `source_type` 字段

## [DEC-005] - 2026-04-02 - 技能命名改为 `article2book`

### 背景

用户希望把技能名从 `article-to-book` 改成 `article2book`，以便与自己的命名习惯保持一致。

### 选项

1. **保留 `article-to-book`**
 - 优点：语义直观
 - 缺点：不符合用户当前希望使用的命名

2. **改为 `article2book`**
 - 优点：更短，更贴近用户指定名称
 - 缺点：需要同步修改目录名和文档引用

### 决策

选择 **选项 2：改为 `article2book`**。

### 理由

1. 技能目录名应与技能名保持一致
2. 用户已经明确指定新的命名方式
3. 当前技能尚处于早期阶段，改名成本很低

### 影响

- `SKILL.md` frontmatter 的 `name` 改为 `article2book`
- 技能目录由 `article-to-book/` 改为 `article2book/`
- `CHANGELOG.md`、`DECISIONS.md`、`TASKS.md` 中的名称引用同步更新

## [DEC-006] - 2026-04-02 - Agent 通读优先，脚本仅作辅助索引

### 背景

用户明确指出，成书判断最重要的是 Agent 对全文内容的阅读理解，而不是依赖脚本先扫目录。脚本可以做索引，但不应该主导主题判断。

### 选项

1. **保留“脚本先扫，再人工抽读”的流程**
 - 优点：更快
 - 缺点：容易把主题判断建立在不充分阅读上

2. **改为“Agent 通读优先，脚本仅作辅助”**
 - 优点：更贴近真正的阅读理解过程
 - 缺点：耗时更高，需要明确批次阅读方法

### 决策

选择 **选项 2：Agent 通读优先，脚本仅作辅助**。

### 理由

1. 成书判断依赖的是母题、论证、重复度和章节潜力，这些都需要全文理解
2. 文件名、摘要和统计字段只能帮助查漏，不能替代阅读
3. 对课程讲稿和逐字稿这类口语素材，尤其需要 Agent 自己判断其可书稿化程度

### 影响

- `SKILL.md` 的主流程改为先通读全部候选文本素材
- 新增 `references/agent-reading-protocol.md`
- 输出模板增加 `processed/agent_reading_notes.md`
- `scripts/build_article_inventory.py` 降级为可选辅助工具

## [DEC-007] - 2026-04-02 - 通读后先做低质量内容排除

### 背景

用户指出，在 Agent 通读完素材后，还应先筛掉一些老旧、浅层、低质量或明显较弱的内容，否则后续目录设计会被噪音拖累。

### 选项

1. **把全部素材都纳入后续聚类**
 - 优点：不容易漏材料
 - 缺点：低质量旧内容会稀释主线，降低成书质量

2. **通读后先做“保留 / 降权 / 排除”筛选**
 - 优点：先做减法，再做结构化，更接近真实编辑流程
 - 缺点：需要明确筛选标准，避免误删有价值旧文

### 决策

选择 **选项 2：通读后先做“保留 / 降权 / 排除”筛选**。

### 理由

1. 一本书不需要收进所有旧内容，而是要留下最能支撑母题的内容
2. 老内容不是问题，老而浅、过时失效、重复且更弱才是问题
3. 先筛掉噪音，后续主题聚类和目录设计会更清晰

### 影响

- `SKILL.md` 新增“通读后先筛选”的明确步骤
- 新增 `references/content-screening-rubric.md`
- 输出模板增加 `00A-排除与降权清单.md`

## [DEC-008] - 2026-04-02 - 默认输出改为单一“书稿策划意见”

### 背景

在真实目录上跑完第一版后，发现虽然多文件分析包足够完整，但用户在首次判断阶段并不需要同时阅读这么多文件。过多拆分文件会放大认知负担，反而削弱“先确认方向”的效率。

### 选项

1. **继续默认输出多文件分析包**
 - 优点：结构完整，可追溯性强
 - 缺点：文件过多，首次阅读成本高

2. **默认只输出一份主意见，支撑文件按需生成**
 - 优点：更贴近真实决策路径，先看结论，再决定是否展开细节
 - 缺点：需要在技能中额外说明哪些文件属于可选支撑

### 决策

选择 **选项 2：默认只输出一份 `书稿策划意见.md`，支撑文件按需生成**。

### 理由

1. 用户在第一阶段真正需要的是一份可判断、可修改、可确认的主意见
2. 素材总表、筛选清单和阅读笔记更适合作为后备依据，而不是默认阅读入口
3. 这样更利于把 skill 接到第二阶段的直接写书动作上

### 影响

- `SKILL.md` 的标准输出调整为“单文件轻交付”
- `references/output-template.md` 改为以 `书稿策划意见.md` 为默认模板
- `processed/agent_reading_notes.md`、素材总表和筛选清单降级为可选支撑文件

## [DEC-009] - 2026-04-02 - 引入“先策划，后直出全书”的两阶段流程

### 背景

用户明确希望先拿到一份整体意见，确认方向后不要再经历冗长中间步骤，而是直接进入全书重构，尽量一步到位地得到完整书稿。

### 选项

1. **技能只负责策划，不负责后续书稿重构**
 - 优点：边界单纯
 - 缺点：用户还要再发起第二套流程，衔接成本高

2. **技能内置两阶段流程：先策划，确认后直接出全书初稿**
 - 优点：更贴近真实使用路径，也更利于复用通读结果
 - 缺点：需要说明阶段切换条件和统稿责任

### 决策

选择 **选项 2：技能内置两阶段流程**。

### 理由

1. 用户需求不是“分析完就停”，而是“分析后立刻可进入重构”
2. 通读与筛选的结果若不继续用于写书，很多中间理解会被浪费
3. 两阶段放在同一 skill 中，更容易统一母题、目录、术语和文风

### 影响

- `SKILL.md` 新增第二阶段：确认后输出 `全书初稿.md`
- `references/output-template.md` 新增 `全书初稿.md` 模板说明
- 允许用 subagent 分章并行起草，但由主 Agent 负责最终统稿

## [DEC-010] - 2026-04-02 - 默认不外显内部分析痕迹

### 背景

在第一版真实交付中，虽然已经把默认输出压缩为单份意见，但筛选细表、阅读笔记和过程性文件仍然容易被继续显性输出，用户首次判断时仍会承受不必要的阅读负担。

### 选项

1. **继续保留过程性文件作为常规可见输出**
 - 优点：可追溯性强
 - 缺点：容易把内部工作痕迹误变成用户默认阅读入口

2. **把过程性文件进一步内化，只在必要时显性输出**
 - 优点：更符合“先看意见、再决定是否展开”的真实使用路径
 - 缺点：需要在主意见中压缩保留足够的筛选依据

### 决策

选择 **选项 2：把过程性文件进一步内化**。

### 理由

1. 用户真正要先判断的是方向，不是阅读完整分析过程
2. 筛选原则和删改动作可以压缩进主意见，不必默认拆成单独文件
3. 这样更利于从第一阶段无缝衔接到第二阶段的整书重构

### 影响

- `SKILL.md` 新增“内部痕迹不默认外显”的判断原则
- `references/output-template.md` 要求把“保留 / 合并 / 排除原则”默认写入 `书稿策划意见.md`
- `附录-排除与降权清单.md` 明确为非默认产物

## [DEC-011] - 2026-04-03 - frontmatter description 收缩为“触发条件 + 核心能力”

### 背景

用户指出 `article2book` 的 `description` 过长，不符合成熟已发布 skill 的写法。对比其他稳定 skill 后，可以看到 `description` 更适合承担“何时触发、能做什么”的职责，而不是重复完整工作流。

### 选项

1. **继续保留长 description**
 - 优点：信息完整
 - 缺点：frontmatter 负担过重，触发信息与执行细节混在一起

2. **改为短 description，只保留触发条件和核心能力**
 - 优点：更符合成熟 skill 的常见写法，也更利于快速理解
 - 缺点：需要把细节继续留在 `SKILL.md` 正文中承载

### 决策

选择 **选项 2：改为短 description**。

### 理由

1. `description` 的主要任务是帮助识别使用时机，而不是承载完整规范
2. 已有的执行细节、边界和工作流在 `SKILL.md` 正文里已经足够完整
3. 收短后更接近成熟 skill 的 frontmatter 风格

### 影响

- `SKILL.md` 的 `description` 调整为“现有素材成书评估 + 书稿策划意见 + 确认后直出全书初稿”的简洁表达
- 版本号更新为 `0.2.2`

## [DEC-012] - 2026-04-03 - 示例与验证记录不保留用户本地绝对路径

### 背景

用户指出，skill 中的示例不应暴露其本地目录路径。该问题不仅出现在 `SKILL.md` 的示例请求里，也会连带影响变更记录中的真实验证表述。

### 选项

1. **保留真实绝对路径**
 - 优点：记录精确
 - 缺点：会把用户本地目录信息固化在技能文档中

2. **改为通用占位或泛化表述**
 - 优点：保留文档可读性和验证事实，同时避免泄露本地路径
 - 缺点：失去少量环境细节

### 决策

选择 **选项 2：改为通用占位或泛化表述**。

### 理由

1. 示例的任务是传达用法，不需要绑定某个真实机器路径
2. 技能级文档应尽量可复用，而不是带着单个用户环境痕迹
3. 验证事实可以保留，但不需要暴露验证时使用的具体本地目录

### 影响

- `SKILL.md` 示例中的素材目录改为 `<素材目录>`
- `CHANGELOG.md` 与 `DECISIONS.md` 中涉及真实验证路径的表述改为泛化描述

## [DEC-013] - 2026-04-03 - 取消 `archive` 回退，永远默认输出到源目录

### 背景

用户明确要求 `article2book` 不再使用技能目录内的 `archive/` 作为回退输出位置，而是始终把结果写回素材所在目录。

### 选项

1. **保留“源目录优先，archive 回退”**
 - 优点：兼容不可写目录和集中存放
 - 缺点：会在 skill 目录内再引入一层输出位置，增加理解负担

2. **永远默认输出到源目录**
 - 优点：结果始终贴着原素材，路径规则简单直接
 - 缺点：若源目录不可写，需要在实际运行时再显式处理异常或由用户指定目录

### 决策

选择 **选项 2：永远默认输出到源目录**。

### 理由

1. 用户希望输出位置稳定且可预期，不再引入额外回退目录
2. `书稿策划输出/` 贴着原素材目录，更符合实际整理和反复修改的工作流
3. `archive/` 会让 skill 自己多出一套中间存放逻辑，当前收益不高

### 影响

- `SKILL.md` 的默认输出目录改为只写“源目录下的 `书稿策划输出/`”
- `scripts/build_article_inventory.py` 不再把 `archive` 视为技能默认生成目录
- 删除技能目录中的 `archive/.gitkeep`

## [DEC-014] - 2026-05-17 - 从成书策划升级为内容资产再组织

### 背景

用户希望 `article2book` 写得更有泛化性。现有版本已经能处理文章、讲稿和逐字稿的成书策划，但默认判断仍以“能不能成一本书”为中心。真实内容资产整理场景中，很多素材更适合转化为课程、小册子、系列文章、实务手册或知识库。

### 选项

1. **继续强化成书路径**
 - 优点：边界清晰，延续既有定位
 - 缺点：容易把不适合成书的素材也强行导向书稿

2. **升级为内容资产再组织，保留成书作为默认主路径**
 - 优点：既保留已有能力，又能覆盖课程、手册、知识库等更自然的转化结果
 - 缺点：需要新增形态判断、素材类型处理和输出模板字段

### 决策

选择 **选项 2：升级为内容资产再组织，保留成书作为默认主路径**。

### 理由

1. 用户的核心需求是把已有内容资产变成更有价值的结构化成果，不一定总是一本书
2. 先判断最佳内容形态，可以减少错误成书、硬拼目录和过度写作
3. 保留 `article2book` 名称与两阶段流程，能兼容既有触发习惯和历史文档

### 影响

- `SKILL.md` 的定位扩展为“内容资产再组织”
- 新增内容形态判断树和快速盘点 / 标准策划 / 深度重构三档模式
- 新增 `references/content-productization-models.md` 和 `references/source-type-handling.md`
- `references/output-template.md` 增加最佳内容形态、不建议形态、转化路径和下一步产物
- `references/book-viability-rubric.md` 增加替代形态建议
- `scripts/build_article_inventory.py` 标记 `.docx`、`.pdf` 为需预处理素材

## [DEC-015] - 2026-05-17 - 正式发布到项目 skills 目录

### 背景

`article2book` 已在私有目录完成内容资产再组织能力升级，用户要求将该技能正式发布到本项目的 `skills/` 目录，并同步 README 文档。

### 选项

1. **继续保留在 `private-skills/`**
 - 优点：可继续内部迭代
 - 缺点：不能作为项目正式技能被安装和发现

2. **复制到 `skills/article2book/` 并升级为正式版本**
 - 优点：符合项目正式发布目录规范，可被 README 和 Marketplace 发现
 - 缺点：需要同步维护公开版本号和项目级入口

### 决策

选择 **选项 2：复制到 `skills/article2book/` 并升级为正式版本**。

### 理由

1. 用户已明确要求正式发布
2. 技能已具备完整的 `SKILL.md`、参考文件、脚本、许可证和技能级文档
3. 正式目录技能应使用 `1.x.x` 版本号，因此公开版本从 `1.0.0` 开始

### 影响

- `article2book` 进入项目正式 `skills/` 目录
- `SKILL.md` 版本号升级为 `1.0.0`
- `CHANGELOG.md` 保留 0.x 私有开发历史，并新增 1.0.0 正式发布记录
- 项目 README 与 Marketplace 清单需要同步添加该技能

## 工作日志

### 2026-05-17 16:51 (Codex)

- **目标**：将 `article2book` 从私有技能发布为项目正式技能
- **操作**：
 - 将私有目录当前版本复制到 `skills/article2book/`
 - 将公开版本号升级为 `1.0.0`
 - 新增正式发布变更记录和决策记录
 - 同步项目 README 与 Marketplace 清单
- **结果**：`article2book` 已具备正式发布目录、公开版本号和项目入口
- **下一步**：在更多真实内容资产目录中回归验证，并补充匿名化样例

### 2026-05-17 16:38 (Codex)

- **目标**：按用户确认的计划，将 `article2book` 泛化为内容资产再组织技能
- **操作**：
 - 更新 `SKILL.md` 的定位、触发描述、工作模式、交付形态判断树和工作流程
 - 新增内容产品形态模型和素材类型处理规则
 - 扩展输出模板与成书可行性量表，要求低分时给出替代形态建议
 - 增强扫描脚本，对 `.docx`、`.pdf` 标记预处理状态
 - 清理发布无关文件，并同步 `CHANGELOG.md`、`TASKS.md`
- **结果**：`article2book` 当前不再只判断“能不能成书”，而是先判断素材最适合转化为书、小册子、课程、系列文章、实务手册、知识库或暂不产品化
- **下一步**：补充匿名化样例，并在多个真实混合目录中做回归验证

### 2026-04-02 21:55 (Codex)

- **目标**：创建一个私有 skill，把零散文章目录整理为可出版书籍的策划底稿
- **操作**：
 - 抽样阅读用户提供的一组真实文章目录，识别主题主线
 - 设计技能定位、标准输出、判断原则与工作流程
 - 新增输出模板、成书评估量表和轻量扫描脚本
 - 创建 `CHANGELOG.md`、`DECISIONS.md`、`TASKS.md`、`LICENSE.txt`、`archive/.gitkeep`
- **结果**：完成 `article2book` 初版，并对一组真实素材目录做了脚本级冒烟验证，成功识别 70 份候选素材；当前技能已可执行主题提炼、成书评估、目录映射与重写补写建议
- **下一步**：用真实文章目录跑一次完整验证，并按结果微调聚类与输出模板

### 2026-04-02 22:00 (Codex)

- **目标**：根据用户反馈，把 skill 的边界从“文章成书”扩展为“现有素材结构化成书”
- **操作**：
 - 重写 `SKILL.md` 的定位、输入范围与不适用场景
 - 将“文章总表”调整为“素材总表”
 - 更新输出模板，使其支持讲稿和逐字稿映射
 - 更新扫描脚本，加入 `.srt`、`.vtt` 和 `source_type`
- **结果**：技能现在明确服务于“已有内容资产再开发”，而不是从零造书
- **下一步**：找一组包含文章和逐字稿的混合目录，验证素材类型区分是否足够好

### 2026-04-02 22:14 (Codex)

- **目标**：按用户要求，将技能名从 `article-to-book` 改为 `article2book`
- **操作**：
 - 更新 `SKILL.md` 的 frontmatter 名称与版本号
 - 在技能级文档中统一替换技能名
 - 将技能目录改名为 `article2book/`
- **结果**：技能名称、目录名和文档引用保持一致
- **下一步**：如需继续使用该 skill，后续均以 `article2book` 为准

### 2026-04-02 22:18 (Codex)

- **目标**：按用户反馈，把 `article2book` 的主流程从“脚本先扫”改为“Agent 通读优先”
- **操作**：
 - 重写 `SKILL.md` 的工作流程和判断原则
 - 新增 `references/agent-reading-protocol.md`
 - 在输出模板中加入 `processed/agent_reading_notes.md`
 - 明确脚本只承担索引和查漏角色
- **结果**：技能的主导能力调整为 Agent 的全文阅读理解，而不是脚本摘要
- **下一步**：拿真实目录跑一版完整通读式输出，检查阅读笔记模板是否顺手

### 2026-04-02 22:51 (Codex)

- **目标**：按用户反馈，把“低质量旧内容排除”加进 `article2book` 的主流程
- **操作**：
 - 在 `SKILL.md` 中加入“通读后先做保留 / 降权 / 排除筛选”的步骤
 - 新增 `references/content-screening-rubric.md`
 - 更新 `agent-reading-protocol.md` 和 `output-template.md`，加入深度判断、筛选结论与排除清单
- **结果**：技能现在会先剔除老而浅、过时失效、重复且更弱的内容，再做主题聚类和目录设计
- **下一步**：用真实目录验证排除标准是否过严或过宽

### 2026-04-02 23:35 (Codex)

- **目标**：根据真实使用反馈，压缩默认输出文件数量，并把技能扩展为“两阶段直达全书”工作流
- **操作**：
 - 在真实目录上完成一次完整策划输出，观察用户阅读成本
 - 将默认交付改为单一 `书稿策划意见.md`
 - 将阅读笔记、素材总表、筛选清单改为可选支撑文件
 - 在 `SKILL.md`、`output-template.md`、`agent-reading-protocol.md` 中加入“用户确认后直接生成 `全书初稿.md`”和 subagent 并行说明
- **结果**：技能的默认体验从“多文件分析包”切换为“先一份主意见，再一步到位重构全书”
- **下一步**：补充匿名化样例，验证简化后的默认模式在更多目录中的适用性

### 2026-04-02 23:55 (Codex)

- **目标**：把“单文件优先”的边界再收紧一层，避免内部分析痕迹继续外显
- **操作**：
 - 在 `SKILL.md` 中新增“默认轻交付 / 结论统一 / 内部痕迹不默认外显”三条判断原则
 - 补充更贴近用户心智的示例请求，明确“先出一份意见，再直接生成全书初稿”
 - 在 `output-template.md` 中把“保留 / 合并 / 排除原则”并入主意见模板，并将筛选清单明确为非默认产物
- **结果**：技能默认交付进一步稳定为“一份主意见 + 按需支撑”，减少首次阅读的认知负担
- **下一步**：继续用真实目录验证单文件意见是否足以支撑用户直接进入第二阶段

### 2026-04-03 00:10 (Codex)

- **目标**：把 `article2book` 的 frontmatter `description` 调整为更成熟的简洁写法
- **操作**：
 - 对比多个成熟 skill 的 `description` 组织方式
 - 将 `description` 收缩为“触发场景 + 核心能力”的两句式表达
 - 同步更新版本记录和最后更新日期
- **结果**：`description` 不再承载完整工作流，frontmatter 风格更接近成熟已发布 skill
- **下一步**：后续如继续公开化或上架，再统一检查其他 frontmatter 字段是否也需要进一步收敛

### 2026-04-03 00:18 (Codex)

- **目标**：清理 skill 示例与文档记录中的用户本地路径痕迹
- **操作**：
 - 将 `SKILL.md` 示例中的绝对路径替换为 `<素材目录>`
 - 将 `CHANGELOG.md` 和 `DECISIONS.md` 中涉及真实验证目录的表述改为泛化描述
- **结果**：skill 示例和文档记录不再暴露用户本地目录信息，同时保留验证上下文
- **下一步**：后续新增示例时继续保持占位写法，避免重新引入本地路径

### 2026-04-03 00:26 (Codex)

- **目标**：按用户要求移除 `archive` 回退输出，统一改为输出到源目录
- **操作**：
 - 修改 `SKILL.md` 的默认输出目录与 Step 1 表述
 - 从扫描脚本忽略目录中移除 `archive`
 - 删除技能目录中的 `archive/.gitkeep`
- **结果**：`article2book` 当前规则已收紧为永远默认输出到源目录，不再使用技能内 `archive/`
- **下一步**：后续如需支持特殊输出位置，应改为显式用户指定，而不是再引入隐式回退目录
